#categories
  %ul    
    - @filter_categories.each do |category|
      %li{ :class => request.path =~ /kategorie\/#{category.id}/ ? "selected" : "not-selected" }
        = link_to category.name, category