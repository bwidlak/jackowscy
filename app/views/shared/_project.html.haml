- unless project.images.blank?
  .project
    %a{:href => "/projects/#{(project.id)}", :style => "background-image: url(#{project.images.first.img_url})"}
      .badge
        .author
          = project.author.name
        .name
          = project.name        
        .price
          = project.price